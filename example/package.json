{
  "name": "once-init-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rimraf ../docs && cp -rf dist ../docs"
  },
  "dependencies": {
    "@types/mockjs": "^1.0.5",
    "axios": "^0.24.0",
    "mockjs": "^1.1.0",
    "once-init": "link:../",
    "p-memoize": "^6.0.1",
    "sass": "^1.47.0",
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}